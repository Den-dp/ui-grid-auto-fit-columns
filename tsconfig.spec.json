{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "target": "ES2019", // downgrade, b/c webpack@4 parser doesn't support syntax of ?? and !. operators
    "types": [
      "jasmine",
      "node"
    ]
  },
  "files": [
    "src/test.ts"
  ],
  "include": [
    "src/**/*.spec.ts",
    "src/**/*.d.ts"
  ]
}
